[package]
name = "swell"
version = "0.1.0"
edition = "2021"
description = "A shell written in Rust"

[[bin]]
name = "swell"
path = "src/main.rs"

[dependencies]
nix = { version = "0.29", features = ["term", "process"] }
